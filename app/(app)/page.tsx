import { FarmScreen } from "@/components/farm/FarmScreen";

// Ферма — головний екран застосунку (основний ігровий цикл: майнинг $HASH).
export default function HomePage() {
  return <FarmScreen />;
}
